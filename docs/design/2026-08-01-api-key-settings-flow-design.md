# API Key 设置流程设计

主页面移除接口配置区块、API Key 输入框和“获取 API Key”跳转链接，只保留模型、尺寸、细节和数量等生成参数。模型选择继续在主页面失焦/切换后自动保存。

设置页面成为唯一的 API Key 配置入口，展示配置状态、API Key 输入框和“获取 API Key”跳转链接。用户在输入框完成输入并失焦后，前端自动调用现有设置接口保存 API Key；不显示单独的保存按钮，也不回显已经保存的完整 Key。

此变更不增加模型选择控件，不修改后端接口或数据库结构。
