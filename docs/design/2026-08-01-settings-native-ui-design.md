# 设置页 Native UI 风格设计

设置页保留当前 GenImage 的浅色背景、文字层级、按钮尺寸和响应式留白，使其与工作台保持一致。

设置区块、输入框和主题选项改用 Native UI 风格：1px 中性浅灰边框、8px 圆角、克制的弱阴影和系统默认交互反馈。按钮继续使用应用现有主/次操作颜色，但描边和边界改为轻量化处理。

仅修改 `.settings-page`、`.settings-section` 及其内部控件的样式；不修改工作台、登录页或任何后端功能。
