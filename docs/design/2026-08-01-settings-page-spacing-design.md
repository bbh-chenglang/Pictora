# 设置页留白设计

设置页内容列在宽屏下最大宽度调整为 720px，并保持水平居中。页面外层使用 48px 水平留白；每个设置区块使用 32px 水平内边距，使接口配置、修改密码和主题区块的内容不贴近边框。

在 860px 以下视口，页面外层改为 24px 水平留白，区块改为 20px 水平内边距；在 520px 以下，页面外层为 16px、区块为 16px。此调整不改变设置页功能、接口或主题状态。
